/** @type {import('next').NextConfig} */
const nextConfig = {};

const localConfig = {
    images: {
        domains: ['localhost'],
    },
    reactStrictMode: false,
};

module.exports = localConfig; 
